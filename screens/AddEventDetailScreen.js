import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

const AddEventDetailScreen = props => {
    return(
        <View>
            <Text>Add this specific event!</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default AddEventDetailScreen;